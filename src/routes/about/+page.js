/** @type {import('./$types').PageLoad} */
export function load() {
  return {
    meta: {
      title: 'About Us - MySite',
      description: 'Learn more about MySite, our mission, and the team behind our success.'
    }
  };
}
